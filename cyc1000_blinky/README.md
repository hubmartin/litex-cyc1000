# CYC1000 LED chaser

The eight on-board LEDs advance approximately once every 0.70 seconds.

```sh
./build.sh
./load.sh
```

`load.sh` configures SRAM only: power cycling restores the prior flash configuration.
